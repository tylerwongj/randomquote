Notes:
npm link  # links the "bin" cmd in package.json to your cmd line
npm publish --access=public  # initial publish for the public
npm publish  # works after you increment the version in package.json - e.g. to "1.0.1"